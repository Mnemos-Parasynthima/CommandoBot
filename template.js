//Template for Commando structure
/*
const { Command } = require('discord.js-commando');

module.exports = class NameCommand extends Command {
  constructor(client) {
    super(client, {
      name: String,
      aliases: Array,
      group: String,
      memberName: String,
      description: String,
      format: String,
      guildOnly: Boolean,
      ownerOnly: Boolean,
      clientPermissions: Array,
      userPermissions: Array,
      nsfw: Boolean,
      throttling: {
        usages: Integer,
        duration: Integer,
      },
      args: [
        {
          key: String,
          prompt: String,
          type: String,
        }
      ],
      patterns: Array<RegExp>,
      guarded: Boolean,
      hidden: Boolean
    });
  }

  run(msg, {  }) {
    //command
  }
};*/